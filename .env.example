# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Cloudinary
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# The WhatsApp business number is NOT an env var — it's stored in the
# database (settings table) and editable at /admin/settings, so it can be
# changed without a redeploy. See supabase/migrations/0002_settings.sql.
